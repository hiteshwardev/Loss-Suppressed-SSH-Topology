# Pinned to the versions used to generate all results and figures.
# (Lower bounds are conservative; the project uses only stable, long-standing APIs.)
numpy>=1.24,<3.0
scipy>=1.10,<2.0
matplotlib>=3.7,<4.0
PyYAML>=6.0,<7.0

# Notebook execution / reproduction (optional but used by `make notebooks`)
jupyter>=1.0
nbconvert>=7.0
nbformat>=5.0
ipykernel>=6.0

# Testing
pytest>=7.0
